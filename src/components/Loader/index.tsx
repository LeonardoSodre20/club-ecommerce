const LoaderAuth = () => {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <h1
        style={{
          color: "#fff",
          fontSize: "4em",
        }}
      >
        Carregando ....
      </h1>
    </div>
  );
};

export default LoaderAuth;
